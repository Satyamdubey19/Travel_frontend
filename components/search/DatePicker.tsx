"use client"

import DatePicker from "@/components/ui/DatePicker"
import type { DatePickerProps } from "@/types/search"

export default function SearchDatePicker(props: DatePickerProps) {
  return <DatePicker {...props} />
}
