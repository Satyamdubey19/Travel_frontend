# Travels Pro frontend — local development template
# This application only needs the backend API origin. Do not copy database,
# OAuth, payment, mail, encryption, or session secrets into this project.
BACKEND_API_URL=http://localhost:4000
